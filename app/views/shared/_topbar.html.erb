<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <%= link_to "Food System", root_path, class: "navbar-brand" %>
    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <%= link_to "Início", root_path, class: "nav-link" %>
          <%= link_to "Pedidos", orders_path, class: "nav-link" if user_signed_in? %>
          <%= link_to "Comidas", foods_path, class: "nav-link" if user_signed_in? %>
          <%= link_to "Restaurantes", restaurants_path, class: "nav-link" if user_signed_in? %>
          <%= link_to "Perfil", edit_user_registration_path, class: "nav-link" if user_signed_in? %>
          <%= link_to "Sair", destroy_user_session_path, method: :delete, class: "nav-link" %>
        <% else %>
          <%= button_to "Logout", destroy_user_session_path, data: { turbo_method: :delete } %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
